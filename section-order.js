(() => {
  // Temporarily disabled: moving live dashboard sections at runtime caused the test page to stop rendering reliably.
  // The loans and assets section remains in its original document position until the layout is moved directly in HTML.
})();
